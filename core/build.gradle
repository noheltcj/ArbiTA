buildscript {
    ext {
        artifact_id = 'core'
    }
}

jar {
    baseName = artifact_id
    version = shared_version
}

compileKotlin {
    kotlinOptions.jvmTarget = jvm_target
}

compileTestKotlin {
    kotlinOptions.jvmTarget = jvm_target
}